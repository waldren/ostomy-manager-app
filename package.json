{
  "name": "ostomy-application",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Dual-platform (mobile + web) patient-facing app for ostomy care tracking.",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=24.0.0 <25",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:unit": "pnpm -r --if-present run test:unit",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "check:env": "bash scripts/check-no-committed-env.sh",
    "dev:reset": "bash scripts/dev-reset.sh",
    "//build:deps": "Every workspace whose consumers typecheck against its BUILT output must be listed here, in dependency order. `packages/ui` was missing and CI caught it: a clean checkout has no `packages/ui/dist`, so `apps/web`'s typecheck could not resolve `@ostomy/ui` — while every local run passed, because a stale dist from an earlier build was still on disk. Adding a package here is not optional bookkeeping; it is the difference between `pnpm verify` meaning something on a fresh clone and meaning nothing.",
    "build:deps": "pnpm --filter @ostomy/core build && pnpm --filter @ostomy/seed build && pnpm --filter @ostomy/ui build",
    "verify:build-artifact": "pnpm --filter @ostomy/api build && node -e \"require('./apps/api/dist/prisma/prisma.service.js'); console.log('built CJS artifact loads: apps/api/dist/prisma/prisma.service.js')\"",
    "verify:core-require": "pnpm --filter @ostomy/core build && node apps/api/scripts/require-core-smoke.cjs && echo \"apps/api (CommonJS) required @ostomy/core (ESM) built output successfully - ADR-0010 proof\"",
    "verify:api-client": "pnpm --filter @ostomy/api api-client:check",
    "verify": "pnpm run check:env && pnpm run format && pnpm run build:deps && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run verify:build-artifact && pnpm run verify:core-require && pnpm run verify:api-client"
  },
  "devDependencies": {
    "@ostomy/config": "workspace:*",
    "eslint": "^9.39.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "prisma"
    ],
    "//overrides": "One React for the whole repo, because `.npmrc` sets node-linker=hoisted. Hoisting puts a single react-dom at the root while a workspace whose range excludes the hoisted react keeps its own nested copy - so components render against one React instance and react-dom's dispatcher is bound to another. That does not error: it renders NOTHING, and packages/ui's suite failed 21 tests with an empty <div /> and no clue why. 19.2.3 is Expo SDK 57's pin and the version React Native 0.86 is tested against, so the native side chooses it and everything else follows. Raise this only in step with an Expo upgrade.",
    "overrides": {
      "react": "19.2.3",
      "react-dom": "19.2.3"
    }
  }
}
