node_modules/
dist/
build/
coverage/
.expo/
# TypeScript project-reference incremental build cache (apps/web's `tsc -b`
# composite build writes these next to the tsconfig, not into dist/).
*.tsbuildinfo
.env*
!.env.example
.envrc
*.log
.DS_Store
.vscode/
# Claude Code: share project agents, skills and settings; keep personal overrides local
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/settings.json

