node_modules/
dist/
build/
coverage/
.expo/
pnpm-lock.yaml
*.min.js
LICENSE
# Generated by @ostomy/api at boot (src/openapi/); not committed.
apps/api/openapi.json
# Generated by Prisma ORM 7's "prisma-client" generator (P1.S3); not
# committed — see apps/api/.gitignore and prisma/schema.prisma's generator
# block.
apps/api/src/generated/
# design-specs/ holds the spec, the ADRs, and design mockups exported from
# other tools. None of it is source we format, and reformatting a mockup
# would produce a diff against a file its author regenerates elsewhere.
design-specs/

# Prettier formats code, not prose.
#
# The Markdown here is documents of record — the SRS, the ADRs (immutable once
# accepted), the agent definitions. Prettier pads Markdown table columns to
# align them, which turns a one-cell edit into a whole-table diff and churns
# files whose git history is supposed to mean something. It buys nothing:
# these are read on GitHub and in editors that render them.
#
# Remove an entry here only if you want that tree auto-formatted.
**/*.md
