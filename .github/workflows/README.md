# CI Workflows

GitHub Actions workflows go here (lint/test/build pipelines, etc.). `pr.yml` — lint, typecheck, test and dependency scan on every pull request and on push to `main`. Runs on GitHub-hosted runners.

The development deploy workflow (self-hosted runner) lands with P1.S2.
