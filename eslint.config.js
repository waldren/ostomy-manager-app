// Root ESLint configuration. The shared rules live in packages/config so every
// workspace lints identically — see packages/config/eslint/index.js.
import config from '@ostomy/config/eslint';

export default config;
