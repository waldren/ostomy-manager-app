# Testing

Test strategy and how to run/write tests for each app and package.

_TBD — cover unit tests (packages/core, FHIR mapping logic), component tests (packages/ui), and end-to-end tests (mobile/web), plus how acceptance criteria in `design-specs/requirements/` map to test cases._
