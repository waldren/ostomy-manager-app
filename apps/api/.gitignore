dist/
openapi.json

# Prisma ORM 7's "prisma-client" generator writes plain TypeScript into the
# source tree by design (see prisma/schema.prisma's generator block) rather
# than into node_modules — but it is still a build artifact, regenerated by
# `pnpm --filter @ostomy/api prisma:generate` (also run automatically by
# `build`/`start:dev`/`typecheck`), never hand-edited, and not committed.
src/generated/
